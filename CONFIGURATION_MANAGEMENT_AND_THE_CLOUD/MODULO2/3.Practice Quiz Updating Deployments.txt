1)What is a production environment in Puppet?
Resp)The parts of the infrastructure where a service is executed, and served to its users.

2)What is the --noop parameter used for?
Resp)Simulating manifest evaluation without taking any actions

3)What do rspec tests do?
Resp)Check the manifests for specific content

4)How are canary environments used in testing?
Resp)As a test environment to detect problems before they reach the production environment

5)What are efficient ways to check the syntax of the manifest? (Check all that apply)
Resp)Run full No Operations simulations, Run rspec tests, puppet parser validate
